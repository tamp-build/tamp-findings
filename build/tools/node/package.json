{
  "name": "tamp-findings-scan-tools",
  "private": true,
  "description": "Node-based scanners the Nuke build shells out to. Lives here, not in an application project, because there is no longer a JavaScript application in this repository — TFND-128 retired web/ and took its devDependencies with it, which silently disabled the accessibility scan.",
  "devDependencies": {
    "@axe-core/cli": "^4.10.1",
    "axe-sarif-converter": "^2.11.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "chromedriver"
    ]
  }
}
