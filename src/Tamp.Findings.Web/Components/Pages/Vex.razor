@page "/c/{Client}/p/{Project}/vex"
@attribute [Authorize]
@inject RouteScope Scope

<PageTitle>VEX — @Project</PageTitle>

<Placeholder Title="VEX statements" Ticket="TFND-99"
             Description="Reachable from any CVE row in the SBOM spine. Lead Dev drafts, InfoSec publishes — that transition is a workflow, not a permission check."
             Scope="Params" />

@code {
    [Parameter] public string Client { get; set; } = "";
    [Parameter] public string Project { get; set; } = "";

    private List<KeyValuePair<string, string?>> Params =>
        [new("client", Client), new("project", Project)];

    protected override void OnParametersSet() => Scope.SetProject(Client, Project);
}
