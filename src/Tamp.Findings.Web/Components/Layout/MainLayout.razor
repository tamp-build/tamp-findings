@inherits LayoutComponentBase

@* Placeholder shell. The real application shell — 238px sidebar, scope card,
   nav groups, the two-row header with the URL chip — is TFND-64 and TFND-65,
   and depends on the router (TFND-63) and the token stylesheet (TFND-61). *@
<main class="shell">
    @Body
</main>
